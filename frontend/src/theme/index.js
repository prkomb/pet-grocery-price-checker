import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    brandColor: {
      royalBlue: "#1E40AF",
      teal: "#34D399",
    },
    BrandTitle: {
      mainColor: "#64FCD9",
    },
  },
});

export { theme };
